let nome = 'Lucas';

console.log(`${nome} nasceu em 1984.`);
console.log(`Em 2000' ${nome} conheceu Maria`);
console.log(`${nome} casou-se com Maria em 2012`);
console.log(`Maria teve 1 filho com ${nome} em 2015.`);
console.log(`O filho de ${nome} se chama Eduardo.`);

let idade; // declaração de variável
idade = 30; // atribuição de valor
console.log(idade);
idade = 20
console.log(idade);

// não podemos criar variáveis com palavras reservadas
// variáveis precisam ter nomes significativos
// não podemos começar com números
// não podemos usar espaços ou traços
// variáveis podem ser declaradas com let, const ou var
// let e const são mais utilizados em JS moderno
// utilizamos cameCase para nomear variáveis em JS
let nomeCompleto = 'Lucas Silva';
// case sensitive , ou seja, letras minúscula e maiúscula fazem diferença
// não podemos redeclarar uma variável com let